import { getCommentsByPostId } from "./services/CommentService";

// nest this component in BlogDetails component 
const CommentDetails = () => {
    return (
        <>
            <p>Comment - email, name, body </p>
        </>
    );
};

export default CommentDetails;